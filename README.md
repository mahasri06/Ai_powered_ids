# AI-Powered Intrusion Detection System (IDS)

This project uses Machine Learning + real-time packet sniffing
to detect network intrusions directly from your terminal.

## How to Run

### Install dependencies
pip3 install -r requirements.txt

### Train the model
python3 train.py

### Run real-time IDS
sudo python3 detector.py
